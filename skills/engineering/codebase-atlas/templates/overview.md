# Overview

One page on what this codebase is and where its edges are. Written once during full generation; revisited only when the stack or the boundaries change.

## Stack

<!-- Languages, frameworks, build tooling, test tooling; one line each. -->

## Entry points

<!-- Where execution enters: CLI commands, HTTP routes, job listeners, library exports. -->

## Feature clusters

<!-- The handful of subsystems a newcomer should learn, in order; one line each. -->

## External boundaries

<!-- Everything the codebase talks to that is not the codebase: databases, HTTP APIs, queues, third-party SDKs. Name the client module that owns each boundary. -->
