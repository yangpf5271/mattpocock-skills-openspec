---
"mattpocock-skills-openspec": patch
---

implement: UI-scenario acceptance becomes a two-way choice, automated browser E2E when the repo has the infrastructure, or a manual acceptance route: one numbered walkthrough per spec scenario (page, action, expected observable result) the user clicks through and reports pass or fail on, failures returning as bug reports. Manual route is the default substitute when no infrastructure exists, a scenario needs human judgment of look and feel, or the user asks. archive-proposal: user-confirmed walkthrough scenarios count as covered, noted as verified by hand.
