---
"mattpocock-skills-openspec": patch
---

Reword the 21 localized skill description prefixes to encode when-to-use (model-invoked) or a picker cue (user-invoked) instead of naming the skill or its output.
